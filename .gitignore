# Secrets - never commit
.env

# Auto-generated at runtime
# NOTE: state.json is intentionally NOT ignored -- it must be committed so
# GitHub Actions remembers which topic is next between daily runs.
poster.log
cron.log

# Python
__pycache__/
*.pyc
.venv/
venv/
